name = "swastik-motors"
compatibility_date = "2024-01-01"
pages_build_output_dir = ".vercel/output/static"

# For Pages Functions (API routes)
[build]
command = "npm run build"

[build.environment]
NODE_VERSION = "18"
NEXT_TELEMETRY_DISABLED = "1"

# Enable Node.js compatibility for MongoDB
node_compat = true
